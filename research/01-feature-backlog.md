# 01 — Бэклог фич

Что ещё можно добавить, сгруппировано по **фазам разработки**. Внутри фазы приоритеты: P0 — высокая ценность/скоро, P1 — важно, P2 — приятно. 🆕 — идеи, добавленные при брейншторме (не из первой версии списка).

Колонка **Реализовано** — версия, в которой фича приехала: ✅ целиком, ◐ частично (детали в заметках), «—» ещё нет.

## Порядок фаз

1. **Фаза 1 — Сейчас (клиент, всё на IndexedDB).** Обкатываем продукт и всю логику локально, без бэка.
2. **Фаза 2 — Бэкенд по архитектуре.** Поднимаем сервисы по `02-backend-architecture.md` (аккаунты, хранилище, брокер, notifications, analytics).
3. **Фаза 3 — Интеграция клиент ↔ бэк.** Подменяем репозитории IndexedDB → API, миграция данных, синк, роли, offline-first.
4. **Фаза 4 — Фичи и улучшения.** То, что имеет смысл только когда всё уже связано (облачный дашборд, Pro, расшаривание).

> **Ключевая предпосылка фазы 1 — выполнена в 0.2.7.** Дети, задачи, награды и настройки уехали из
> констант в IndexedDB: сид из дефолтов с сохранением идентификаторов, на которые ссылается история,
> дозаполнение недостающих строк и полей без перезаписи сохранённого, `seedVersion` у настроек.
> Доступ к данным изолирован в `entities/*/api` и pinia-сторах, поэтому в фазе 3 подмена
> IndexedDB → API будет точечной, а UI-редакторы переделывать не придётся.

---

## Фаза 1 — Сейчас (клиент, IndexedDB)

| Фича | Приоритет | Реализовано | Заметки |
|---|---|---|---|
| **Перевод фронта на FSD + TypeScript** | P0 🆕 | ✅ 0.2.0 | **Предпосылка перед разработкой фазы 1.** Привести архитектуру к Feature-Sliced Design (слои `app`/`pages`/`widgets`/`features`/`entities`/`shared`) и дальше строго соблюдать. Текущее `components`/`composables`/`db`/`config` раскладываем по слоям: репозитории (`completionsRepo`/`spendsRepo`/`walletRepo`) → `entities/*/api`; `TasksBoard`/`GoalMeterBoard`/`RewardsPanel` → `widgets`; «принять»/«наградить»/серия → `features`; дети/задачи/награды → `entities`; общий UI/утилиты → `shared`. **В рамках миграции переходим с JS на TypeScript.** Entity-модели описываем zod-схемами (тип + runtime-валидация, единый источник; валидируют в т.ч. импорт JSON), без class-transformer. Типы/клиент с бэка автогенерим после релиза бэкенда — правка точечная, только в `entities/*/api`. Даёт чистоту и предсказуемость до наслоения фич. **Сделано в 0.2.0:** 8 сущностей, 7 фич, 4 виджета; состояние на pinia; strict TS без `any` и `@ts-ignore`, `vue-tsc` в CI; межсущностные связи через `@x`; правила слоёв — в `CLAUDE.md` и `src/README.md`. |
| **Родрежим: редактирование наград** | P0 | — | Имя, стоимость, иконка; добавить/удалить/переупорядочить. Стоимость — снимок в списаниях уже есть, история не поедет. |
| **Родрежим: редактирование задач** | P0 | — | Имя, иконка, баллы, вкл/выкл, порядок. `active:false` вместо удаления (не ломать историю). |
| **Родрежим: редактирование детей** | P0 | — | Имя, аватар (выбор/загрузка фото в IndexedDB), недельная/копилочная цель. Добавить/удалить. |
| **Настройка бонуса** | P0 | ◐ 0.2.5 | Вкл/выкл «доп. балл за все задачи дня», размер бонуса, какие задачи входят в «все». Сейчас захардкожено (+1 за 3 задачи). **Правило уехало из кода в `settings` и читается оттуда; экрана настройки нет.** |
| **Серия (streak) за задачи** | P1 🆕 | ✅ 0.2.6 | N дней подряд со всеми задачами — чтобы дети не филонили. В родрежиме настраивается: вкл/выкл, длина серии, **баллы за серию и/или награда за серию** (оба опциональны). Считается по `completions`; при пропуске дня серия обнуляется, у ребёнка показываем прогресс (🔥 5/7). Детали модели наград и выдачи — подраздел «Модель наград» ниже. **Сделано: расчёт из отметок, недельная веха с наградой, бейдж 🔥, экран серии, поздравление, своя анимация. Настройка вехи из интерфейса — вместе с редакторами.** |
| **Модель наград: видимость и источники** | P1 🆕 | ◐ 0.2.5 | Награда получает независимые оси: `purchasable` (можно купить за баллы) и `visibility` (`shop`/`streak`/`hidden`). Позволяет создать награду, привязать к серии и не показывать в общем списке — при этом та же награда может параллельно продаваться за баллы. Всё настраивает родитель. Детали — подраздел ниже. **Оси `purchasable` и `visibility` есть в схеме и работают: приз за серию не показывается в магазине. Управления из родрежима пока нет.** |
| **Онбординг / стартовый экран** | P1 | — | Первый запуск: сразу в родрежиме → установка пароля → мастер «дети → задачи → награды». Пустое состояние вместо дефолтных Тимофей/Даниил. Поверх — подсветка-туториал (coach marks). До завершения мастера обычный режим заперт (нельзя оставить открытый родрежим ребёнку). |
| **Родрежим: сброс пароля (локально)** | P1 | — | Кнопка «сбросить» → очищаем текущий пароль → повторный шаг установки нового. Без бэка и почты. Email-вариант — фаза 3. |
| **Автолок родрежима по таймауту** | P1 🆕 | — | Родрежим сам выходит после N минут простоя. Иначе легко оставить планшет с открытым родрежимом. |
| **Галерея пресет-аватарок + выбор иконок** | P1 | — | Выбор аватарки ребёнка из набора рисованных заглушек и иконки задач/наград. Пресеты — публичные генерик-ассеты (не ПДн); реальное фото — опционально и приватно (см. `04`). |
| **Назначение задач на ребёнка** | P1 | — | Не все задачи для всех: у задачи — для кого показывать (сейчас общий список на обоих). |
| **Расписание задач по дням недели** | P1 🆕 | — | Какая задача в какие дни активна (уборка — по выходным, чтение — каждый день). Иначе список одинаков всю неделю. |
| **Экран статистики/истории** | P1 | — | По неделям/месяцам на ребёнка: заработал/потратил, стрики, графики. Данные уже в `completions`/`spends`. |
| **Разовые задачи / квесты** | P2 🆕 | — | Родитель на лету добавляет одноразовую задачу с баллами («помог с уборкой» +3), не засоряя постоянный список. |
| **Отмена «Принять» (undo)** | P2 🆕 | — | Тост «Отменить» N секунд после начисления — на случай промаха. Сейчас откат только через сброс недели. |
| **PIN вместо пароля** | P2 🆕 | ✅ 0.2.5 | Числовой PIN для входа в родрежим — быстрее на планшете, чем текстовый пароль. **Шесть цифр, цифровая клавиатура через `inputmode`, автоотправка. Смена PIN из интерфейса — вместе с редакторами.** |
| **Лимиты/кулдаун на награды** | P2 🆕 | — | «Мороженка Кафе» не чаще раза в неделю, дневной лимит YouTube и т.п. Правило на награду. |
| **Копилка-цель с картинкой** | P2 🆕 | — | Ребёнок копит на конкретную «большую» награду с прогресс-баром и картинкой — мотивация копить, а не тратить сразу. |
| **Витрина желаний (wishlist)** | P2 🆕 | — | Ребёнок предлагает награду → родитель одобряет/оценивает в баллах. |
| **Штрафы / отрицательные баллы** | P2 | — | Родрежим: снять баллы за проступок (запись в леджер со знаком минус). |
| **Геймификация** | P2 | — | Уровни, ачивки. Движок начислений на клиенте. Базовые стрики вынесены в отдельную настраиваемую «Серию» выше. |
| **Режим планшета / киоск** | P2 🆕 | — | Крупные кнопки, блок случайного выхода из PWA, экран «только сегодня» для детей. |
| **Темы/звуки/анимации** | P2 | ◐ 0.2.6 | Светлая тема, палитры, звук/хаптика на «Принять» и покупку (конфетти уже есть). **Анимации: пять пресетов конфетти случайно и отдельный за серию. Светлая тема, палитры и звук — нет.** |
| **Правила копилки** | P2 | — | Настройка шага копилки (сейчас 50), режим «недельный сброс vs накопительный». |
| **Праздники / каникулы** | P2 🆕 | — | Дни, когда автосброс недели и обязательные задачи не действуют. |
| **Локализация (i18n)** | P2 | — | Сейчас только ru. Цель — ru / en / es. Quasar language packs + вынести строки в словари. |
| **Экспорт CSV** | P2 🆕 | — | Помимо JSON — выгрузка истории в CSV для родителя (открыть в Excel/Sheets). |

### Известные баги 🆕

| Баг | Где | Заметки |
|---|---|---|
| **Поздравление повторяется при правке истории** | `features/track-streak` | Идентификатор приза содержит дату срабатывания вехи (`streak:{ребёнок}:{веха}:{день}`). Если правка истории в родрежиме сдвигает, какой день становится седьмым, старый приз удаляется, а новый считается свежевыданным — и модалка поздравления открывается снова. Воспроизводится при откате серии с 8 до 7. Лечится либо идентификатором без даты (номер цикла внутри забега), либо отметкой «уже показывали» на выданном призе. |

### Детали: серия + модель наград 🆕

Расширяем награду из «товара за баллы» в сущность с источником и видимостью. Три оси независимы, всё настраивает родитель.

**Награда:**

```js
{
  id, name, icon,
  points,               // цена в баллах
  purchasable: true,    // ось «экономика»: можно купить за баллы
  visibility: 'shop'    // ось «видимость»: 'shop' | 'streak' | 'hidden'
}
```

- `shop` — в общем списке «Награды».
- `streak` — не в общем списке, показывается как приз серии.
- `hidden` — не видна ребёнку, только родителю (сюрприз).

Оси независимы → любые комбинации: обычный товар; только приз серии (`purchasable:false, visibility:'streak'`); **и покупается, и даётся за серию** (`purchasable:true, visibility:'shop'` + привязка к серии); секретная награда (`purchasable:false, visibility:'hidden'`).

**Серия = список milestone'ов** (расширяемо на 3/7/30 дней):

```js
{ id, enabled, days, points: 0, rewardId: null }   // points и rewardId — оба опциональны
```

За серию можно назначить баллы, награду, или и то и другое. `rewardId` ссылается на любую награду, в т.ч. на ту, что параллельно продаётся в shop.

**Выдача (переиспользуем существующее, без новых таблиц):** при достижении серии — (1) если есть `points`, начисляем бонус в леджер; (2) если есть `rewardId`, пишем в `spends` с `cost: 0` и `source: 'streak'`. Награда попадает в историю кошелька, баланс не трогаем. Добавляем только поле `source` в `spends` (`'purchase' | 'streak'`) — чтобы отличать покупку от выданного за серию и корректно откатывать.

**Решения (зафиксировано):**

1. **Повторяемость серии:** настройка milestone'а `once` / `repeat`, **по умолчанию `repeat`** — после достижения серия обнуляется и приз можно заработать снова (мотивация не заканчивается после первого раза).
2. **Per-child vs общие:** **начинаем с общих** — одна конфигурация серии/наград на всех детей. Назначение per-child — улучшение на потом.
3. **Дни серии при расписании задач:** считаем **только дни, где у ребёнка есть обязательные задачи**. Все выполнил → серия +1; выполнил не все → серия рвётся (в 0); день без назначенных задач — **нейтральный** (не считается и не обнуляет). Связывает «Серию» с «Расписанием задач по дням недели».

---

## Фаза 2 — Бэкенд по архитектуре

Поднимаем сами сервисы (детали — `02-backend-architecture.md`). Пока без привязки к клиенту.

| Сервис / блок | Реализовано | Заметки |
|---|---|---|
| **Аккаунты + вход (Google/Yandex SSO)** | — | Идентификация семьи, привязка устройств. |
| **Хранилище (источник правды) + бэкапы** | — | БД + регулярные резервные копии; не зависеть от локального кеша браузера. |
| **Доменный API** | — | Награды/задачи/дети/`completions`/`spends` — те же сущности, что в репозиториях клиента. |
| **Серверный баланс + античит** | — | Единый баланс на всех устройствах, начисления валидируются на сервере. |
| **Notifications-сервис** | — | Расписания напоминаний per-timezone, шаблоны, push / email / Web Push. Кормится из брокера. |
| **Mail-сервис** | — | Транзакционная почта (в т.ч. сброс пароля по email). Отдельно или как часть Notifications. |
| **Analytics-сервис + OLAP (ClickHouse)** | — | Продуктовые/поведенческие события → агрегаты и дашборды. На старте события можно в Postgres. |
| **Брокер/очередь на Redis** | — | Streams для событий, BullMQ для отложенных задач. Асинхрон, развязка сервисов, сглаживание пиков (пользователи в разных TZ). |

---

## Фаза 3 — Интеграция клиент ↔ бэк

| Фича | Реализовано | Заметки |
|---|---|---|
| **Подмена репозиториев IndexedDB → API** | — | Слой доступа уже изолирован — меняем реализацию, UI не трогаем. |
| **Миграция локальных данных в облако** | — | Импорт существующего IndexedDB в аккаунт при первом входе. |
| **Offline-first + разрешение конфликтов** | — | Клиент работает оффлайн, синк при сети; правила слияния (last-write-wins/merge по сущности). |
| **Синхронизация между устройствами** | — | Родитель с телефона, дети с планшета — общий стейт. |
| **Несколько опекунов + роли** | — | Оба родителя/бабушки в одной семье; роли родитель / со-опекун / ребёнок. |
| **Сброс пароля по email** | — | Письмо со ссылкой сброса на почту родителя — теперь есть аккаунты + mail-сервис. |
| **Push-уведомления и напоминания** | — | «Сделай дела», «неделя закрывается» — серверный планировщик + push-канал, подключённый к клиенту. |

---

## Фаза 4 — Фичи и улучшения

Имеют смысл, когда всё уже связано.

| Фича | Реализовано | Заметки |
|---|---|---|
| **Облачный дашборд родителя (веб)** | — | Тренды по семье в браузере, а не только в приложении. |
| **Подписка Pro** | — | Облако/семьи/приватность за деньги (биллинг). |
| **Расшаривание прогресса (read-only)** | — | 🆕 Ссылка бабушке/дедушке посмотреть успехи ребёнка. |
| **Мультисемья / несколько профилей** | — | 🆕 Несколько семей на одном аккаунте/устройстве. |
| **Аудит действий родителя** | — | 🆕 История изменений (кто снял баллы, поменял награду) — прозрачность между опекунами. |
| **Умные напоминания / инсайты** | — | 🆕 На основе аналитики: «Даниил три дня не читал», лучшее время для напоминаний. |
| **Глубокая геймификация (сервер)** | — | Ачивки/уровни/сезоны, которые считаются на сервере и переживают переустановку. |
| **Интеграция с календарём** | — | 🆕 Расписание задач ↔ семейный календарь. |

### Крупная отдельная фича: разделение клиентов 🆕

**Совсем потом.** Развести один PWA на **родительский** и **детский** клиенты — большой отдельный трек (разные экраны, роли, устройства, доступы). Всё ниже зависит от этого разделения и без него не делается:

- **Детский вход (ограниченный экран)** — отдельный вход ребёнка без доступа к родрежиму, только свои задачи и баланс.
- **Очередь одобрения** — ребёнок отметил задачи на своём клиенте → родителю пуш «принять» → подтверждение с телефона родителя.
